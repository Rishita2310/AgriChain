const axios = require('axios');

async function test() {
  try {
    // Generate a JWT token for the farmer to test the endpoint
    // We will just do a direct DB call instead of full axum route to see if there's any serialization panic
  } catch (err) {
    console.error(err);
  }
}
test();
